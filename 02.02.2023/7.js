/* Користувач вказує обсяг флешки у ГБ. Програма має під-
рахувати скільки файлів розміром 820 МБ вміститься на
флешку. */
"use strict"

const gb = 1024;

let mb = +prompt(`Бажаєте Перевірити обсяг флешки, а також конвертувати у МВ?\nТоді, Введіть обсяг у ГБ`)
let r = mb * gb;
alert (`Ось, обсяг вашої флешки складає ${r} MB`);